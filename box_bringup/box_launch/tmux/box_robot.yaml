session_name: box

before_script: "../scripts/initialize_session.sh"

windows:
- window_name: grand_tour_box_robot
  layout: tiled
  shell_command_before:
    - >
     echo "Test"

  options:
    pane-border-status: top
  panes:
    - roslaunch box_launch rviz.launch
    - shell_command:
      - df -h $HOME
      focus: true
  